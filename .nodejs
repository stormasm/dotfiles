
alias n='node'

# yarn
alias y='yarn'
alias yi='yarn install'
alias yb='yarn build'

alias vs='vue serve'

alias cnp='env | grep NODE_PATH'

### nana / tauri
alias nprt='npm run tauri'
alias nprtd='npm run tauri dev'
alias nrtd='npm run tauri dev'

### run rollup locally, must have a rollup.config.js
alias nrc='npx rollup --config'
### install rollup locally
alias nroll='npm install rollup --save-dev'

### pnpm commands
alias pn='pnpm'

### npm commands

alias nprb='npm run build'
alias nprd='npm run dev'
alias nprdeploy='npm run deploy'
alias npi='npm install'
alias npiy='npm init -y'
alias npmrun='npm run'
alias nr='npm run'
alias nps='npm start'
alias npt='npm test'
alias npw='npm run watch'

### Javascript Pretty commands

alias jsb='js-beautify $1 -o $1'

#
# To use prettier you must be in the same
# directory as the file you are pretty printing
#
# pj filename
#
alias jp='prettier --write'
alias pj='prettier --write'
