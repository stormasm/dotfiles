
alias n='node'
alias vs='vue serve'

alias cnp='env | grep NODE_PATH'

### npm commands

alias nprb='npm run build'
alias nprd='npm run dev'
alias nprdeploy='npm run deploy'
alias npi='npm install'
alias npiy='npm init -y'
alias npr='npm run'
alias nps='npm start'
alias npt='npm test'
alias npw='npm run watch'

### Javascript Pretty commands

alias jsb='js-beautify $1 -o $1'

#
# To use prettier you must be in the same
# directory as the file you are pretty printing
#
# pj filename
#
alias jp='prettier --write'
alias pj='prettier --write'

